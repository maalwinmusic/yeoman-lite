import expect from "../../testUtils/unexpected-react";
import React from "react";
import { __NAME__ } from "./__NAME__";

describe("__NAME__", () => {
  it("renders correctly", () => {
    const wrapper = <__NAME__ />;
    expect(wrapper, "to satisfy", <div className="__NAME__"></div>);
  });
});
